{
  "name": "@interfirm/configs",
  "version": "0.0.3",
  "description": "Manage common config files for interfirm projects",
  "scripts": {
    "postinstall": "bin/configs-sync",
    "lint": "eslint --fix .",
    "test": "echo TODO: Check all source files exist"
  },
  "bin": {
    "configs-sync": "bin/configs-sync"
  },
  "license": "MIT",
  "devDependencies": {
    "eslint": "^3.19.0",
    "eslint-config-interfirm": "^2.1.0"
  }
}
